import React from "react";
import { Outlet } from "react-router-dom";

function Authentication() {
  return (
    <Outlet />
    /*     <p>
      Authentication Page is responsible to show login or registration
      components
    </p> */
  );
}

export default Authentication;
